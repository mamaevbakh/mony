# Bubble data type slug for packages
VITE_BUBBLE_PACKAGE_TYPE_SLUG=package

# Relationship field on the package pointing to the parent service (change if different)
VITE_BUBBLE_PACKAGE_SERVICE_FIELD=service
